# Core (standard library — no install needed):
#   tkinter, sqlite3, csv, json, threading, logging, hashlib, os, pathlib

# Required third-party packages
bcrypt>=4.0.1           # Secure password hashing (preferred over SHA-256)
rapidfuzz>=3.0.0        # Fast fuzzy string matching for employee name lookup
openpyxl>=3.1.0         # Excel (.xlsx) import and export

# Optional — only needed when running in REST server mode
Flask>=3.0.0            # Lightweight REST server (server.py)
Flask-HTTPAuth>=4.8.0   # Token-based auth for the REST API
requests>=2.31.0        # HTTP client used by Tkinter app in REST mode

# Optional — makes large CSV/XLSX parsing faster
pandas>=2.0.0           # Not required; openpyxl used by default

# Packaging
pyinstaller>=6.0.0      # Build Windows .exe
